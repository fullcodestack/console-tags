{
  "name": "@fullcodestack/console-tags",
  "version": "0.9.0",
  "description": "Add colored CSS-styled tags to the console log.",
  "author": "Steven van Cauter <steven@fullcodestack.com>",
  "license": "MIT",
  "engines": {
    "node": ">= 10.13"
  },
  "dependencies": {
    "tslib": "^1.9.3"
  },
  "devDependencies": {
    "@types/node": "^11.10.5",
    "prettier": "^1.16.4",
    "rimraf": "^2.6.3",
    "tslint": "^5.13.1",
    "tslint-config-prettier": "^1.18.0",
    "tslint-microsoft-contrib": "^6.1.0",
    "tsutils": "^3.8.0",
    "typescript": "^3.3.3"
  },
  "scripts": {
    "clean": "rimraf coverage build tmp",
    "build": "tsc -p tsconfig.release.json",
    "build:watch": "tsc -w -p tsconfig.release.json",
    "lint": "tslint -t stylish --project \"tsconfig.json\"",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  }
}
